#include <stdio.h>
#define SetBit(A,k)     (A[(k/32)] |= (1 << (k%32)))  

//day 1
int part1(){
    int data[]
    for (int i = 0; i < size; i++) {
        
    }
    return 0;
}

int main(){
    int *numbers[4] = {1,19,3,2};
    printf(part1(numbers, 4));
    return 0;
}

